export enum MODEL {
  FAVORITE = "Favorite",
  PROFILE = "Profile",
  SIMULATOR = "Simulator",
}

export enum COLLECTION {
  FAVORITE = "favorites",
  PROFILE = "profiles",
  SIMULATOR = "simulators",
}
